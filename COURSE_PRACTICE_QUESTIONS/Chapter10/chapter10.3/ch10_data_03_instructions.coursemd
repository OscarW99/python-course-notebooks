<div style="width:100%; background-color:#226c61; color:white; padding:10px; text-align:center; font-size:20px;">
    <strong>Chapter 10: Data Manipulation</strong>
</div>

<div style="text-align:center; font-size:28px; font-weight:bold;">
    NumPy Broadcasting and Vectorization
</div>
<br>
<div style="font-size:18px;">

# Question 1
Using broadcasting, add the number 5 to this 2x2 array and print the result:

```python
array = np.array([[1, 2], [3, 4]])
```
<br>

# Question 2
Create a 2x3 array and multiply all its elements by 2 using broadcasting.
<br></br>

# Question 3
Using broadcasting, add a 1D array `[10, 20, 30]` to each row of a 2x3 array filled with ones.
<br></br>

# Question 4
Demonstrate vectorization by calculating the square of each element in this array without using a loop:

```python
values = np.array([1, 2, 3, 4, 5])
```

# Question 5
Use vectorization to multiply these two arrays element-wise:

```python
array1 = np.array([1, 2, 3, 4])
array2 = np.array([10, 20, 30, 40])
```
<br></br>