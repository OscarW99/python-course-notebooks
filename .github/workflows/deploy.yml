name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Prepare deployment
        run: |
          # Create temporary directory for deployment
          mkdir -p temp_deploy
          
          # Copy all HTML files from Chapter1 and Chapter2
          mkdir -p temp_deploy/Chapter1 temp_deploy/Chapter2
          cp COURSE_PRACTICE_QUESTIONS/Chapter1/*.html temp_deploy/Chapter1/
          cp COURSE_PRACTICE_QUESTIONS/Chapter2/*.html temp_deploy/Chapter2/
          
          # Copy Skulpt files maintaining relative paths
          mkdir -p temp_deploy/skulpt-dist-master
          cp -r skulpt-dist-master/* temp_deploy/skulpt-dist-master/
          
          # Create .nojekyll file
          touch temp_deploy/.nojekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./temp_deploy
          publish_branch: gh-pages
          force_orphan: true
